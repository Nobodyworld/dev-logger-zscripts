# Agent Integration Helpers

Automation clients and AI agents can rely on the helpers in this package to
interact with the zscripts CLI safely. The metadata exported by
`cli_adapter.py` mirrors the live command surface exposed through
`python cli.py`.

Consult `agents/AGENTS.md` for contribution rules and `README.md` at the project
root for usage guidance.
